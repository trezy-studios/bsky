/**
 * Validates an rKey string.
 *
 * @param {string} rkey The rKey to be parsed.
 * @returns {string} The rKey.
 */
export function validateRKEY(rkey) {
	// TODO: Validate the rKey.

	return rkey
}
